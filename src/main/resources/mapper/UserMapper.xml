<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zebrarfid.demo.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" parameterType="string" resultType="com.zebrarfid.demo.entity.User">
        SELECT id, username, password, nickname, status
        FROM test_user
        WHERE username = #{username} AND status = 1
    </select>

    <!-- 根据用户名查询用户ID -->
    <select id="selectUserIdByUsername" parameterType="string" resultType="long">
        SELECT id
        FROM test_user
        WHERE username = #{username} AND status = 1
    </select>

</mapper>
